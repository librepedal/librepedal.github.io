name = "librepedal-proximidad"
main = "worker.js"
compatibility_date = "2024-11-01"

[[kv_namespaces]]
binding = "RATE_LIMIT_AUTH"
id = "87af0c17403445f78737975fd0b5b7c3"

# Secretos requeridos (nunca en este archivo, se configuran con `wrangler secret put`):
#   FIREBASE_CLIENT_EMAIL  -- de firebase-service-account.json, campo "client_email"
#   FIREBASE_PRIVATE_KEY   -- de firebase-service-account.json, campo "private_key" (con los \n literales)
#   FIREBASE_PROJECT_ID    -- de firebase-service-account.json, campo "project_id" (hoy: librepedal-cb983)
